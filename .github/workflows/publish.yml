name: Workflow

on: push

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo hi
      - run: python -V
      - run: pwd
      - run: ls
      - run: sudo apt update
      - run: pip install selenium
      - run: python open_ymail.py
